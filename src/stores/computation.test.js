'use strict';

import CalculatorStore from './calculator_store';

describe('Computation', () => {
  describe("@addition", () => {
    test('it works', () => {
      expect(1).toBe(1);
    })
  })
});
